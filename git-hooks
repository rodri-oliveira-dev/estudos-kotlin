var today = new Date();
var targetDay = today.getDate() + 2; // Adiciona dois dias à data atual

$(".table")
  .find("th")
  .each(function (i, e) {
    if (i > targetDay) {
      var thText = $(e).find("abbr").text();
      var tdText = $(".table").find("tfoot td").eq(i).text();

      $(e).html(thText + "<br>" + tdText);
    } else {
      $(e).remove(); // Remove a coluna da tabela
    }
  });
