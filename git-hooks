#!/bin/bash

branch_pattern="^feature/"

current_branch=$(git symbolic-ref --short HEAD)

if [[ $current_branch =~ $branch_pattern ]]; then
    exit 0
else
    echo "Erro: O nome da branch deve começar com 'feature/'."
    exit 1
fi
